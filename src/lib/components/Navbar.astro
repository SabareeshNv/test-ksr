---
const nav = [
  { text: "Articles", link: "/article" },
  { text: "Tools", link: "/tools" },
  { text: "Downloads", link: "/downloads" },
  { text: "About", link: "/about" },
  { text: "Contact", link: "/contact" },
];
---

<!-- <header class="bg-dark py-6">
  <nav class="wrapper grid gap-2 md:grid-cols-2">
    <a
      href="/"
      class="flex items-center justify-center gap-1 pb-3 md:justify-start md:pb-0"
    >
      <span class="text-2xl font-bold text-gray-100">
        Kerala Service <span class="text-brand-bright">Rules</span></span
      ></a
    >

    <ul
      class="flex flex-wrap items-center justify-evenly md:justify-end md:gap-8 lg:gap-12"
    >
      {
        nav.map((item) => (
          <li>
            <a
              href={item.link}
              class="hover:text-brand-bright text-lg text-gray-100 transition-colors"
            >
              {item.text}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</header> -->
<header class="bg-dark py-6">
  <nav class="wrapper nav-grid">
    <a
      href="/"
      class="flex items-center justify-center gap-1 pb-3 md:justify-start md:pb-0"
    >
      <span class="text-2xl font-bold text-gray-100">
        Kerala Service <span class="text-brand-bright">Rules</span></span
      ></a
    >

    <ul
      class="flex flex-wrap items-center justify-evenly md:justify-end md:gap-8 lg:gap-12"
    >
      {
        nav.map((item) => (
          <li>
            <a
              href={item.link}
              class="hover:text-brand-bright text-lg text-gray-100 transition-colors"
            >
              {item.text}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</header>

<style>
  .nav-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: 1fr;

    @media (min-width: 768px) {
      grid-template-columns: 1fr 2.4fr;
    }
  }
</style>
